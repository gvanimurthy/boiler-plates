'use strict';

const config = require('../gulp-config');
const path = require('path');

module.exports = {
	"rules": {
		"quotes": 0,
		"no-empty": 2,
		"no-duplicate-case": 2,
		"valid-typeof": 2,
		"curly": 2,
		"no-unused-vars": 1,
		"comma-dangle": 2,
	}
};